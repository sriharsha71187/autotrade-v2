# Core
python-dotenv>=1.0.0
anthropic>=0.40.0

# Voiceover (free default: gTTS). Optional premium backends loaded lazily.
gTTS>=2.5.0

# Visuals
Pillow>=10.0.0

# YouTube upload (Google Data API v3)
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Optional premium providers (uncomment to enable):
# elevenlabs>=1.0.0        # higher-quality TTS  (ELEVENLABS_API_KEY)
# openai>=1.40.0           # TTS + image generation (OPENAI_API_KEY)
#
# System dependency (NOT pip-installable): ffmpeg must be on PATH.
#   macOS:  brew install ffmpeg
#   Ubuntu: sudo apt-get install ffmpeg
