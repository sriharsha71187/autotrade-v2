<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.autotrade.caffeinate</string>

    <!-- Keep the Mac awake so the cycle scheduler keeps ticking and Telegram/SSH
         stay reachable. -i prevent idle sleep, -m prevent disk sleep, -s prevent
         system sleep (effective only on AC power — keep it plugged in). KeepAlive
         restarts caffeinate if it ever dies, so sleep-prevention is durable rather
         than a fragile one-off process. NOTE: lid-closed sleep on AC is best
         guaranteed by also running: sudo pmset -c disablesleep 1 -->
    <key>ProgramArguments</key>
    <array>
        <string>/usr/bin/caffeinate</string>
        <string>-i</string>
        <string>-m</string>
        <string>-s</string>
    </array>

    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>

    <key>StandardOutPath</key>
    <string>/dev/null</string>
    <key>StandardErrorPath</key>
    <string>/Users/nirvaan/autotrade_error.log</string>
</dict>
</plist>
